{
  "name": "seegak",
  "version": "0.1.0",
  "private": true,
  "description": "High-performance biology data visualization library",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "clean": "pnpm -r clean",
    "lint": "pnpm -r lint",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
